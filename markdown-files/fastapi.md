# Ruth-Deploy

####### TBD

```bash
ruth deploy -h host/address -p  port/number -m path/to/model
```

parameters 

```bash 
- h, --host: host address
- p, --port: port number
- m, --model: path to model
```
